% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/R6Nomogram.R
\name{R6Nomogram}
\alias{R6Nomogram}
\title{Nomogram R6 Object}
\description{
\code{R6Nomogram} builds and plots a nomogram from a fitted regression
model (e.g. \code{lm}, \code{glm}, \code{coxph}, ...). Given the model and
the data used to fit it, the object computes, for each predictor (or
interaction term) in the model, an axis that maps predictor values to a
common "points" scale. The points scale is in turn related back to the
model's linear predictor and to the fitted response, so that the
nomogram can be read by summing points across predictors and converting
the total back to a predicted response.
}
\details{
The construction of a nomogram proceeds through a small pipeline of
steps, all of which are run automatically by \code{initialize()} by default
(and can
also be re-run individually if axes need to be adjusted):
\enumerate{
  \item \code{new} or \code{initialize} - initialize object and store 
         model, terms, and newdat in the object.
  \item \code{PopulateVars()} - extract variable names, term-level
        predictions, and response predictions from the model and data.
  \item \code{create.x()} - build sorted, de-duplicated predictor values
        and the corresponding points for each axis.
  \item \code{shift()} - shift each axis so that point values start at a
        convenient origin (by default, 0).
  \item \code{scale()} - rescale all axes so that the maximum number of
        points is a convenient round number (by default, 100).
  \item \code{pretty()} / \code{pretty.y()} - choose "pretty" tick
        locations for the predictor axes and for the total
        points / linear predictor / response axis.
}
Once built, \code{plot()} draws the nomogram, and \code{tables()} returns
the same information as a list of data frames (e.g. for use outside of a
plot).
}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{model}}{The fitted model object (e.g. from \code{lm}, \code{glm}, 
\code{coxph}, etc.) used to construct the nomogram.}

    \item{\code{newdata}}{The data frame used to build the nomogram, either supplied
by the user or extracted from \code{model}.}

    \item{\code{var.names}}{Character vector of the variable names appearing in the
model formula (excluding the response).}

    \item{\code{x.names}}{Character vector of names for each predictor/interaction
axis on the nomogram (interaction terms are joined with \code{":"}).}

    \item{\code{x.labels}}{Character vector of axis labels used when plotting
(defaults to \code{x.names}).}

    \item{\code{orig.x}}{Data frame of the original predictor variable values
extracted from \code{newdata}.}

    \item{\code{orig.terms}}{Data frame of the model's term-level (partial)
predictions, one column per term.}

    \item{\code{orig.response}}{Vector of unique predicted response values
(response scale).}

    \item{\code{x.vals}}{List, one element per axis, of the unique, sorted predictor
(or interaction) values.}

    \item{\code{x.points}}{List, one element per axis, of the points corresponding
to \code{x.vals}.}

    \item{\code{x.pretty.vals}}{List of "pretty" axis tick values used for plotting.}

    \item{\code{x.pretty.points}}{List of point positions corresponding to
\code{x.pretty.vals}.}

    \item{\code{x.y.offsets}}{List controlling the vertical offset/justification of
axis labels when plotting.}

    \item{\code{scale.c}}{Numeric scaling constant relating points back to the
linear predictor scale.}

    \item{\code{constant}}{The model intercept/offset constant, adjusted as points
are shifted and scaled.}

    \item{\code{total.points}}{Numeric vector of total points corresponding to the
unique fitted values.}

    \item{\code{linear.predictor}}{Numeric vector of linear predictor values
corresponding to \code{total.points}.}

    \item{\code{pretty.total.points}}{Numeric vector of "pretty" total-point tick
values for the response axis.}

    \item{\code{pretty.linear.predictor}}{Numeric vector of linear predictor values
matching \code{pretty.total.points}.}

    \item{\code{pretty.response}}{Numeric vector of response values matching
\code{pretty.total.points}.}

    \item{\code{plot.lp}}{Logical; whether to plot a separate linear-predictor axis
in addition to total points and response.}

    \item{\code{v.pos.x}}{Numeric vector of vertical plotting positions for the
predictor axes.}

    \item{\code{v.pos.r}}{Numeric vector of vertical plotting positions for the
total points / linear predictor / response axes.}

    \item{\code{options}}{List of plotting options: \code{tik.len}, \code{txt.pos},
\code{points.nint}, \code{signif.digits}, \code{text.par},
\code{line.par}, and \code{tick.par}.}

    \item{\code{points.lab}}{Label for the "Points" axis.}

    \item{\code{total.points.lab}}{Label for the "Total Points" axis.}

    \item{\code{lp.lab}}{Label for the "Linear Predictor" axis.}

    \item{\code{resp.lab}}{Label for the "Response" axis.}

    \item{\code{tp.range}}{Numeric vector giving the range of total points used to
position the response axis.}

    \item{\code{verbose}}{Logical; whether to print progress messages.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-R6Nomogram-initialize}{\code{R6Nomogram$new()}}
    \item \href{#method-R6Nomogram-PopulateVars}{\code{R6Nomogram$PopulateVars()}}
    \item \href{#method-R6Nomogram-create.x}{\code{R6Nomogram$create.x()}}
    \item \href{#method-R6Nomogram-shift}{\code{R6Nomogram$shift()}}
    \item \href{#method-R6Nomogram-scale}{\code{R6Nomogram$scale()}}
    \item \href{#method-R6Nomogram-pretty}{\code{R6Nomogram$pretty()}}
    \item \href{#method-R6Nomogram-pretty.y}{\code{R6Nomogram$pretty.y()}}
    \item \href{#method-R6Nomogram-plot}{\code{R6Nomogram$plot()}}
    \item \href{#method-R6Nomogram-grconvertX}{\code{R6Nomogram$grconvertX()}}
    \item \href{#method-R6Nomogram-tables}{\code{R6Nomogram$tables()}}
    \item \href{#method-R6Nomogram-clone}{\code{R6Nomogram$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-initialize}{}}}
\subsection{\code{R6Nomogram$new()}}{
  Construct a new \code{R6Nomogram}. Runs the full construction
pipeline (loading data, populating variables, creating axes,
shifting, scaling, and prettying) up to the requested \code{steps}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$new(
  model,
  newdata,
  verbose = TRUE,
  steps = Inf,
  type.terms = "terms",
  type.response = "response"
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{model}}{A fitted model object (e.g. from \code{lm}, \code{glm},
or \code{coxph}).}
      \item{\code{newdata}}{Optional data frame to use instead of the data stored
in/with \code{model}. If missing, the data is recovered from
\code{model$data}, the \code{data} argument of \code{model$call},
or \code{model.frame(model)}, in that order.}
      \item{\code{verbose}}{Logical; print progress messages for each step.}
      \item{\code{steps}}{Numeric; how many construction steps to run (useful for
stopping partway through, e.g. to inspect or adjust axes by hand
before continuing). Defaults to \code{Inf}, i.e. run all steps.}
      \item{\code{type.terms}}{The \code{type} argument passed to
\code{predict()} to obtain term-level predictions.}
      \item{\code{type.response}}{The \code{type} argument passed to
\code{predict()} to obtain response-scale predictions.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The object itself, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-PopulateVars"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-PopulateVars}{}}}
\subsection{\code{R6Nomogram$PopulateVars()}}{
  Extract variable names, term-level predictions, and response
predictions from \code{model} and \code{newdata}, and use them to
populate \code{x.names}, \code{x.labels}, \code{orig.x},
\code{orig.terms}, \code{orig.response}, \code{constant},
\code{total.points}, and \code{linear.predictor}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$PopulateVars(
  model = self$model,
  newdata = self$newdata,
  type.terms = "terms",
  type.response = "response"
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{model}}{A fitted model object. Defaults to \code{self$model}.}
      \item{\code{newdata}}{A data frame of predictor values. Defaults to
\code{self$newdata}.}
      \item{\code{type.terms}}{The \code{type} argument passed to
\code{predict()} to obtain term-level predictions.}
      \item{\code{type.response}}{The \code{type} argument passed to
\code{predict()} to obtain response-scale predictions.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The object itself, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-create.x"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-create.x}{}}}
\subsection{\code{R6Nomogram$create.x()}}{
  Build, for each axis (predictor or interaction term), the unique,
sorted predictor values (\code{x.vals}) and their corresponding
points (\code{x.points}), derived from \code{orig.x} and
\code{orig.terms}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$create.x()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The object itself, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-shift"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-shift}{}}}
\subsection{\code{R6Nomogram$shift()}}{
  Shift the points for one or more axes by a constant offset. By
default, each axis named in \code{w} is shifted so that its minimum
point value becomes 0; \code{total.points} and \code{constant} are
updated to match so that the overall model is unaffected.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$shift(w = self$x.names, v, update.constant = TRUE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{w}}{Character vector of axis names (from \code{x.names}) to
shift. Defaults to all axes.}
      \item{\code{v}}{Numeric vector of shift amounts, one per element of
\code{w} (matched by name if named). If missing, each axis is
shifted so that the minimum number of points is 0.}
      \item{\code{update.constant}}{Logical; if \code{TRUE} (the default),
\code{total.points} and \code{constant} are updated so totals
remain consistent with the shifted axes.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The object itself, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-scale"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-scale}{}}}
\subsection{\code{R6Nomogram$scale()}}{
  Rescale all axes (and \code{total.points}) so that the largest point
value across all axes equals \code{max.points}. \code{scale.c} is
updated so that points can still be converted back to the linear
predictor scale.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$scale(max.points = 100)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{max.points}}{The desired maximum number of points. Defaults to
100.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The object itself, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-pretty"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-pretty}{}}}
\subsection{\code{R6Nomogram$pretty()}}{
  Choose "pretty" tick locations for one or more predictor axes, by
interpolating (via a natural spline of points on values) the points
corresponding to each chosen tick value. Results are stored in
\code{x.pretty.vals} and \code{x.pretty.points}. Factor axes are
left as-is (every level is used as a "pretty" value).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$pretty(w = self$x.names, v)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{w}}{Character vector of axis names (from \code{x.names}) to
process. Defaults to all axes.}
      \item{\code{v}}{Controls the chosen tick values for each axis in \code{w}.
If missing, ticks are chosen automatically via
\code{axisTicks()}. If a single number, it is used as the
\code{nint} argument to \code{axisTicks()}. If a list, each
element gives the tick values for the corresponding axis (matched
by name). If a numeric vector, it is used directly as the tick
values for every axis in \code{w}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The object itself, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-pretty.y"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-pretty.y}{}}}
\subsection{\code{R6Nomogram$pretty.y()}}{
  Choose "pretty" tick locations for the total points / linear
predictor / response axis, by interpolating (via a natural spline of
response on total points) the response corresponding to each chosen
tick value. Results are stored in \code{pretty.total.points},
\code{pretty.linear.predictor}, and \code{pretty.response}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$pretty.y(v)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{v}}{Controls the chosen total-points tick values. If missing,
ticks are chosen automatically via \code{axisTicks()}. If a single
number, it is used as the \code{nint} argument to
\code{axisTicks()}. Otherwise, used directly as the tick values.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The object itself, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-plot"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-plot}{}}}
\subsection{\code{R6Nomogram$plot()}}{
  Plot the nomogram. Draws the predictor axes (\code{plot.x}) and/or
the total points / linear predictor / response axes (\code{plot.y})
using base graphics, with appearance controlled by \code{self$options}
(\code{text.par}, \code{line.par}, \code{tick.par}, etc.). If
\code{predict} is supplied, a prediction line is drawn connecting the
predicted points on each predictor axis to the corresponding total on
the response axis.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$plot(plot.x = TRUE, plot.y = TRUE, predict, ...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{plot.x}}{Logical; draw the predictor axes. Default \code{TRUE}.}
      \item{\code{plot.y}}{Logical; draw the total points / linear predictor /
response axes. Default \code{TRUE}.}
      \item{\code{predict}}{Optional named list or data frame of predictor values
for which to draw a prediction line/segments on the nomogram.}
      \item{\code{...}}{Additional graphical parameters, passed to \code{par()}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The object itself, invisibly.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-grconvertX"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-grconvertX}{}}}
\subsection{\code{R6Nomogram$grconvertX()}}{
  Convert a value from one nomogram axis scale to the points scale.
For \code{from = "Total Points"}, converts a total-points value to
its plotting position on the points axis. For any other axis name,
converts a predictor value (numeric or factor level) to its points
value via spline interpolation (numeric axes) or direct lookup
(factor axes).
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$grconvertX(x, from = "Total Points", verbose)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{x}}{Value(s) to convert.}
      \item{\code{from}}{Character; the axis to convert from. Either
\code{"Total Points"} (the default) or one of \code{x.names}.}
      \item{\code{verbose}}{Logical; print progress messages. Defaults to
\code{self$verbose}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    The converted value(s), or \code{NA} (with a warning) if
  \code{from} does not match \code{"Total Points"} or any axis name.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-tables"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-tables}{}}}
\subsection{\code{R6Nomogram$tables()}}{
  Summarize the nomogram as a list of data frames: one data frame per
predictor axis (giving \code{x.pretty.vals} and
\code{x.pretty.points}), plus a final \code{Response} data frame
giving \code{pretty.total.points}, \code{pretty.linear.predictor},
and \code{pretty.response}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$tables()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A named list of data frames.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-R6Nomogram-clone"></a>}}
\if{latex}{\out{\hypertarget{method-R6Nomogram-clone}{}}}
\subsection{\code{R6Nomogram$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{R6Nomogram$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
